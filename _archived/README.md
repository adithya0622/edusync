# Archived Files

This folder contains legacy code that is no longer part of the active system but kept for reference.

## Files

### main.py
- **Purpose**: Desktop GUI application using Tkinter
- **Status**: ❌ ARCHIVED (replaced by web-based React + FastAPI system)
- **Why Archived**: 
  - The new system uses a React frontend (localhost:3000) instead of Tkinter GUI
  - The new system uses FastAPI backend (localhost:8080) instead of direct Python calls
  - Provides identical functionality through modern web interface
- **When to Use**: Only if you need to maintain the legacy desktop application
- **Restoration**: If needed, copy back to root directory

## Note
All archived files are Git-tracked. You can restore any file from Git history if needed.

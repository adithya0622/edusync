{
  "buildCommand": "pip install -r requirements.txt",
  "outputDirectory": "",
  "framework": null,
  "routes": [
    {
      "src": "/.*",
      "dest": "app.py"
    }
  ],
  "env": {
    "FLASK_ENV": "production",
    "SECRET_KEY": "your-secret-key"
  }
}